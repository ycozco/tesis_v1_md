# =============================================================================
# requirements.txt — Sistema Integrado de Supervisión Operativa Agroexportadora
# Tesis UNSA — Yoset Cozco Mauri
# Versión: 1.0 — 2026-05-17
# =============================================================================
#
# Uso:
#   python -m venv .venv
#   .venv\Scripts\activate     (Windows) o source .venv/bin/activate (Linux/macOS)
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# Reproducibilidad: las versiones están fijadas (==) para garantizar resultados
# idénticos entre máquinas. La semilla aleatoria principal es 42 (seeds
# secundarias 43..47 para variabilidad estadística).
# =============================================================================

# --- Núcleo científico ---
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.0

# --- Modelos predictivos (capa 1) ---
xgboost==2.0.3
lightgbm==4.3.0
catboost==1.2.5

# --- Detección de anomalías (capa 2) ---
pyod==1.1.3

# --- Explicabilidad (capa 3) ---
shap==0.45.0

# --- Generación de reportes LLM+RAG (capa 4) ---
anthropic==0.34.2          # Claude API
openai==1.40.0             # Alternativa
sentence-transformers==2.7.0   # Embeddings para retrieval
rank-bm25==0.2.2           # BM25 retrieval ligero

# --- Optimización de hiperparámetros ---
optuna==3.6.1

# --- Estadística y pruebas de hipótesis ---
statsmodels==0.14.2
pingouin==0.5.4            # Pruebas no paramétricas y tamaño de efecto

# --- Métricas adicionales ---
rouge-score==0.1.2         # ROUGE para evaluación de reportes

# --- Visualización ---
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0

# --- Notebook y análisis interactivo ---
jupyter==1.0.0
ipykernel==6.29.5

# --- Servidor web (visor de tesis Flask) ---
flask==3.0.0
markdown==3.5.1
pymdown-extensions==10.5
requests==2.31.0

# --- Utilidades de desarrollo ---
python-dotenv==1.0.1       # Carga de variables de entorno (API keys)
tqdm==4.66.4               # Barras de progreso
pyyaml==6.0.1              # Config YAML

# --- Reproducibilidad y testing ---
pytest==8.2.2
pytest-cov==5.0.0

# =============================================================================
# Versión de Python recomendada: 3.11.x
# (Compatibilidad verificada en 3.11, 3.12. 3.14 puede requerir ajustes.)
# =============================================================================
